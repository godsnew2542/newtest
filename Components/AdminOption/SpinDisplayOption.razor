@if (CountNo == -1)
{
	<Spin />
}
else
{
	<a style="font-size:larger">
		@Text
	</a>
}

@code {
	[Parameter] public int CountNo { get; set; } = -1;
	[Parameter] public string? Text { get; set; } = null;
}
